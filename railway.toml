# Legacy Docker configuration, retained for existing consumers.
# The current CulinaryOS Railway services use dashboard-managed Railpack.
# Do not attach this file to those services; see docs/DEPLOYMENT.md section 5.
# Build context for either approach is the full repository root.

[build]
  builder = "dockerfile"
