[build]
  builder = "nixpacks"
  buildCommand = "npm install && npm run build"

[deploy]
  startCommand = "npx serve dist -l 3005"
  restartPolicyType = "on_failure"
  restartPolicyMaxRetries = 3

[[services]]
  [services.variables]
    PORT = "3005"
