# Superseded by .github/workflows/ci.yml (pnpm / apps/* monorepo).
# Kept as a no-op workflow name so old badges/links do not 404 confusingly.
name: Build (legacy)

on:
  workflow_dispatch:

jobs:
  noop:
    name: Redirected to ci.yml
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the CI workflow (ci.yml) for the TypeScript monorepo."
