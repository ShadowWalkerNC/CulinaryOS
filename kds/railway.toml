[build]
  builder = "dockerfile"
  dockerfilePath = "Dockerfile.kds"

[deploy]
  healthcheckPath = "/health"
  healthcheckTimeout = 30
  restartPolicyType = "on_failure"
  restartPolicyMaxRetries = 3
